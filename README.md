# HEllo lets see
## if this works
And how so.

---
I need to highlight these **very important words**.

```
int i = -1;
char *hello = "hello world"
while (++i < strlen(hello))
	printf("%c", hello[i]);
printf("\n")
```
